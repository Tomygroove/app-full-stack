const express = require('express');
const router = express.Router();
const products = require('../controllers/productsController');

router.post('/products', users.product);


module.exports = router;